
document.addEventListener("DOMContentLoaded", () => {
  const resa = document.getElementById("resa-innehall");
  // Här kan dynamiskt innehåll läggas till senare
  resa.innerHTML += "<p>(Ingen sparad historik ännu)</p>";
});
